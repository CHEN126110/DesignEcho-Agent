# 安全策略

## 支持范围

项目仍处于快速演进阶段。安全修复优先应用于默认分支 `main`；尚未发布稳定版本时，不承诺维护历史提交。

## 报告漏洞

请不要通过公开 Issue 报告以下内容：

- API 密钥、令牌或其他凭据泄露
- 任意代码执行、路径穿越或文件覆盖
- 未授权 Photoshop 写入或权限绕过
- WebSocket、IPC、浏览器桥或 Provider 请求中的安全问题
- 可能暴露客户素材、项目路径或模型上下文的问题

请使用仓库的 **Security → Report a vulnerability** 私密报告入口。报告应包含受影响提交、最小复现步骤、影响范围，以及可行的缓解建议（如有）。

维护者会尽快确认报告。在修复可用前，请不要公开披露可利用细节。

## 凭据与素材

项目不会要求你把 API 密钥、客户素材、PSD/PSB、Eagle 库导出或本地模型提交到 Git。若发现凭据已进入 Git 历史，请立即撤销并轮换凭据；仅删除当前文件不足以消除历史泄露。
